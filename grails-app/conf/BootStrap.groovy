import org.where2pair.grails.GormOpenPeriod
import org.where2pair.grails.GormVenue

class BootStrap {

    def init = { servletContext ->
//		GormVenue venue = new GormVenue(latitude: 1.0, longitude: 0.1)
//		venue.addToOpenPeriods(day: 6, openHour: 12, closeHour: 30)
//		venue.save()
	}
    
	def destroy = {
    }
}
